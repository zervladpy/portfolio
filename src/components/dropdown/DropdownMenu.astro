<div
    id="dropdown-menu"
    class="flex flex-col gap-1 absolute top-full right-0 mt-1 scale-y-0 group-hover:scale-y-100 origin-top duration-200 rounded-lg p-1"
>
    <slot />
</div>

<style>
    div {
        background: var(--color-header-bg);
        border: 1px solid var(--color-header-border);
        backdrop-filter: blur(12px);
    }
</style>
