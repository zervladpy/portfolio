---
import Dropdown from "@components/dropdown/Dropdown.astro";
import DropdownTrigger from "@components/dropdown/DropdownTrigger.astro";
import DropdownMenu from "@components/dropdown/DropdownMenu.astro";
import DropdownMenuItem from "@components/dropdown/DropdownMenuItem.astro";

interface Props {
    side?: "top" | "bottom";
}

const { side = "bottom" } = Astro.props;
---

<Dropdown class="theme-dropdown">
    <DropdownTrigger class="theme-trigger" aria-label="Toggle theme">
        <svg class="icon-sun" xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/>
            <line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
            <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/>
            <line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
            <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="icon-moon" xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
    </DropdownTrigger>
    <DropdownMenu class="theme-menu" side={side}>
        <DropdownMenuItem value="light">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="5"/>
                <line x1="12" y1="1" x2="12" y2="3"/>
                <line x1="12" y1="21" x2="12" y2="23"/>
                <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
                <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
                <line x1="1" y1="12" x2="3" y2="12"/>
                <line x1="21" y1="12" x2="23" y2="12"/>
                <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
                <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
            </svg>
            Light
        </DropdownMenuItem>
        <DropdownMenuItem value="dark">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
            </svg>
            Dark
        </DropdownMenuItem>
        <DropdownMenuItem value="system">
            <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="2" y="3" width="20" height="14" rx="2"/>
                <line x1="8" y1="21" x2="16" y2="21"/>
                <line x1="12" y1="17" x2="12" y2="21"/>
            </svg>
            Auto
        </DropdownMenuItem>
    </DropdownMenu>
</Dropdown>

<script>
    const STORAGE_KEY = "theme";
    const html = document.documentElement;
    const media = window.matchMedia("(prefers-color-scheme: light)");
    const dropdowns = document.querySelectorAll<HTMLElement>(".theme-dropdown");

    type Mode = "light" | "dark" | "system";

    function getStoredMode(): Mode {
        const stored = localStorage.getItem(STORAGE_KEY);
        return stored === "light" || stored === "dark" ? stored : "system";
    }

    function resolveTheme(mode: Mode): "light" | "dark" {
        return mode === "system" ? (media.matches ? "light" : "dark") : mode;
    }

    function render(mode: Mode) {
        const resolved = resolveTheme(mode);

        if (resolved === "light") {
            html.setAttribute("data-theme", "light");
        } else {
            html.removeAttribute("data-theme");
        }

        dropdowns.forEach((dropdown) => {
            const sun = dropdown.querySelector<HTMLElement>(".icon-sun");
            const moon = dropdown.querySelector<HTMLElement>(".icon-moon");
            if (sun) sun.style.display = resolved === "light" ? "block" : "none";
            if (moon) moon.style.display = resolved === "dark" ? "block" : "none";

            dropdown.querySelectorAll<HTMLElement>("[data-dropdown-item]").forEach((option) => {
                option.setAttribute("aria-checked", String(option.dataset.value === mode));
            });
        });
    }

    function setMode(mode: Mode) {
        if (mode === "system") {
            localStorage.removeItem(STORAGE_KEY);
        } else {
            localStorage.setItem(STORAGE_KEY, mode);
        }
        render(mode);
    }

    dropdowns.forEach((dropdown) => {
        dropdown.querySelectorAll<HTMLElement>("[data-dropdown-item]").forEach((option) => {
            option.addEventListener("click", () => {
                setMode(option.dataset.value as Mode);
            });
        });
    });

    media.addEventListener("change", () => {
        if (getStoredMode() === "system") render("system");
    });

    render(getStoredMode());
</script>

<style>
    .theme-trigger {
        position: relative;
        padding: 5px;
        border-radius: 6px;
        color: var(--color-foreground-muted);
        transition: color 200ms ease;
    }

    .theme-trigger:hover {
        color: var(--color-foreground);
    }

    .icon-moon {
        display: none;
    }
</style>
